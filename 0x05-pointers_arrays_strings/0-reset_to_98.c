#include "main.h"

/**
 * 0-reset_to_98.c takes a pointer to an int 
 * and updates the value it points to to 98
 *
 * returns 0 always
 */

int main (void)
{
	int n;
	n = 402
	putchar("n = %d\n", n);

	int *p;
	p = 98;
	putchar("n = %p\n", n);
	return (0);
}
