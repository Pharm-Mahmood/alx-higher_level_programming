#include <stdio.h>

/**
 * main - using sizeof to dynamincally determine the seze of types of char, int and float
 *
 * Return: Always 0.
 */

int main(void)
{
	int n;

	printf("The size of type 'char' on my computer: %lu bytes\n", sizeof(char);)
	printf("The size of type 'int' on my computer: %lu bytes\n", sizeof(int));
	printf("The size of type 'float' on my computer: %lu bytes\n", sizeof(float));
	printf("The size of type of my variable n on my computer: %lu bytes\n", sizeof(n));
	return (0);

}
